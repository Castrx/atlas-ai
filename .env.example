# Copie este arquivo para .env e ajuste os valores conforme necessário.
# .env nunca deve ser commitado (ver .gitignore).

# Porta em que o servidor HTTP escuta
PORT=3001

# Ambiente de execução: development | test | production
NODE_ENV=development

# Adaptação temporária: usamos a OpenRouter (API compatível com o padrão
# OpenAI) em vez da OpenAI diretamente, para evitar custo enquanto
# testamos com um modelo gratuito.

# Chave da API da OpenRouter. Obrigatória — a aplicação não sobe sem ela.
# NUNCA commite uma chave real aqui. Preencha apenas no seu .env local.
OPENROUTER_API_KEY=

# URL base da API da OpenRouter. Opcional — tem um default no código.
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Modelo usado nas chamadas à OpenRouter. Opcional — tem um default no código.
OPENROUTER_MODEL=google/gemma-4-26b-a4b-it:free

# Integração real com o Atlas ERP (Fase 3b). Todas obrigatórias — a
# aplicação não sobe sem elas. NUNCA commite valores reais aqui.

# URL base da API do Atlas ERP (sem barra final), ex.: http://localhost:8080
ATLAS_ERP_BASE_URL=

# Credenciais de uma conta de serviço role USER do Atlas ERP (criada via
# POST /auth/register — que sempre cria USER, nunca ADMIN). Nunca use uma
# conta ADMIN aqui: as 6 tools são somente leitura e USER já é suficiente.
ATLAS_ERP_SERVICE_EMAIL=
ATLAS_ERP_SERVICE_PASSWORD=

# Hardening de segurança da Fase 6 (ver ADR-025 no PROJECT_SPEC.md). Todas
# opcionais — se omitidas, a aplicação sobe com os defaults seguros do código.

# Rate limiting em memória, por IP, aplicado só em /api. Default: 30
# requisições a cada 60000ms (60s).
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=30

# Origem exata liberada para CORS, ex.: http://localhost:5173. Deixe vazio
# (default) para não enviar nenhum header de CORS — só requisições
# same-origin funcionam a partir de um navegador.
CORS_ALLOWED_ORIGIN=
